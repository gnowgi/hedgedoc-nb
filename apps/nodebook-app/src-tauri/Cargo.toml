[package]
name = "nodebook"
version = "0.1.0"
description = "nodeBook - CNL knowledge graph editor"
authors = ["nodeBook developers"]
edition = "2021"
license = "AGPL-3.0-only"

[lib]
name = "nodebook_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
