# General settings
HD_BASE_URL="https://hedgedoc.example.com"
# Bind on all interfaces so the reverse proxy (caddy) container can reach the
# backend. Upstream defaults to 127.0.0.1, which yields a 502 behind the proxy.
HD_BACKEND_BIND_IP="0.0.0.0"

# Database settings
HD_DATABASE_TYPE="postgres"
HD_DATABASE_HOST="db"
HD_DATABASE_PORT="5432"
HD_DATABASE_NAME="hedgedoc"
HD_DATABASE_USERNAME="hedgedoc"
HD_DATABASE_PASSWORD="change_me"

# Uploads
HD_MEDIA_BACKEND_TYPE="filesystem"
HD_MEDIA_BACKEND_FILESYSTEM_UPLOAD_PATH="uploads/"

# Auth
HD_AUTH_LOCAL_ENABLE_LOGIN="true"
HD_AUTH_LOCAL_ENABLE_REGISTER="true"
HD_AUTH_SESSION_SECRET="generate_with_openssl_rand_hex_32"

# Note permissions
HD_NOTE_PERMISSIONS_DEFAULT_EVERYONE="write"

# nodeBook text analysis (```nodeBook-analyze``` fences)
# Preferred: the spaCy NLP microservice, run as the in-network 'nlp' compose
# service. Use the service name + its container port 8000.
HD_NLP_SERVICE_URL="http://nlp:8000"
# Build-context path for the 'nlp' service: a checkout of
# github.com/gnowgi/nodebook-nlp (clone it as a sibling of this repo).
NODEBOOK_NLP_PATH="../../nodebook-nlp"
# Fallback hosted LLM API, used ONLY when HD_NLP_SERVICE_URL is empty.
# Anthropic or any OpenAI-compatible endpoint.
HD_LLM_API_URL=""
HD_LLM_API_KEY=""
HD_LLM_MODEL="claude-sonnet-4-20250514"
